# 開発系統の統合台帳

更新日: 2026-09-15。方針: 一つのアプリへ段階的に統合する。

## 統合開始時の作業場所と公開版（2026-09-11）

- 今後の開発場所: `MFRM_Python_Application`。
- 開発ブランチ: `integration/unified-app`。
- 公開ブランチ: `main`。確認時の GitHub HEAD は
  `d8558fc6b4c05d96e4c13a0d067212fde6423b59`、公開表示は `0.2.16-beta`。
- 開発側の `0.2.15-beta` は継承した表示であり、公開版との新旧比較には使わない。
  統合版のバージョン変更は公開準備時に行う。
- `MFRM_Python_Application_working` は原本保全用。今後の変更は開発ブランチに集約する。
  独自の画面・診断・外部連携をすべて移したわけではないため、削除しない。
- 統合開始時点では GitHub への push、公開 `main` への merge、デプロイを行っていなかった。2026-09-13 の公開準備記録は末尾を参照。

## 保全した履歴

| 参照 | 内容 |
|---|---|
| `archive/public-main-20260911` | 確認した公開版 `d8558fc` |
| `archive/local-main-20260911` | 整理前のローカル `main`、`e729f19` |
| `archive/analysis-20260911` | 分析系統の未コミット実装・テスト・検証プロトコルを含む `3cec72d` |
| `archive/working-20260911` | `_working` の未コミット変更も含む `b4dd534` |

二系統の共通祖先は `ce4dd03`。公開版と分析系統の単純な merge は11ファイル、
`_working` と分析系統の merge は15ファイルで競合した。これはファイル数であり、
解消すべき論点や競合ブロックの数ではない。いずれも自動統合済みとは扱わない。

退避先はローカルの `backups/consolidation_20260911/`。
各系統の `history.bundle`、`files.tar.gz`、`manifest.json`、`tracked.patch`、
`status.z` を保存した。manifest は各ファイルのサイズと SHA-256 を保持し、
tar 内の全ファイルを読み直して一致を確認した。

| 原本 | 退避したファイル数 | 合計バイト数 |
|---|---:|---:|
| 分析系統 | 95,371 | 1,603,599,549 |
| `_working` | 149 | 5,859,219 |

対象は Git の追跡ファイルと非無視の未追跡ファイル。仮想環境、キャッシュ、
無視済み秘密設定などを含むディレクトリ全体のバックアップではない。
原本ファイルは移動・削除していない。

分析系統の source snapshot は560ファイルを保存している。
残る94,811ファイルは主に検証結果であり、元の `validation/` と tar に保持する。
その既存結果ディレクトリと vendor packet ZIP のみをローカル
`.git/info/exclude` に列挙し、ソース変更と実験結果を分けた。
この除外は公開リポジトリには伝播しない。

## 統合状況と順序

| 段階 | 状態 | 内容と完了条件 |
|---|---|---|
| 0: 保全・作業系統の整理 | 完了 | 公開版を固定、原本と未コミット成果を検証付きで退避、開発ブランチを一本化 |
| 1: 事前設計の計算基盤 | 完了 | `mfrm_app/simulation/` 14ファイルと既存テスト6ファイルを取り込み、172テスト通過 |
| 2: 公開版の改善を照合 | 未着手 | 公開版の遅延読み込み、Standard実行の計算量制御、設定保存、ダウンロード生成を分析系統へ適用。統計的な出力保護・AnalysisID・日英画面の回帰を確認し、JAX高速化の基準測定へ接続 |
| 3: 事前設計の画面 | 未着手 | `_working` の `mfrm_app/ui/design_planner.py` を共通の入口・Help・状態管理へ接続。画面テストと実ブラウザ確認を通す |
| 4: 適合度計算と表示 | 未着手 | `mfrm_app/diagnostics/` の式・極端反応・raw/display方針を現在の判定規則と照合。既存推定・raw値・丸め前分類を変えないことを検証 |
| 5: 外部検証との連携 | 未着手 | `mfrm_app/external_validation/` の privacy・ConQuest 契約と既存TAM照合を再利用。書き出し、ローカル結果の検証付き読取、推定対象を揃えた比較の順に進め、通常画面から外部推定器を自動実行しない |
| 6: リリース資格 | 0.2.17-betaの公開完了。全統合の資格は未完了 | 既存の出力保護を維持してclean checkout CIと公開起動を確認。未統合機能の統計ゲート・検証成果の復元・正式なアクセシビリティ受入は個別に残す |

段階1は `b4dd534` からファイル内容を変更せず移植した。既存の分析コード、
画面、推定法セレクターには接続変更を加えていない。事前設計基盤の内容は、
評定負担・費用、決定的割付、接続性、構造的な追加評定案、科学条件と乱数の契約。
得点生成、反復推定、標準誤差改善、必要サンプルサイズ推奨は含まない。
画面と統計推論の完成を、計算基盤のテスト成功から推定しない。

元の設計説明は `archive/working-20260911:docs/sample_size_design_mvp.md`、
外部連携の説明は同ブランチの `docs/conquest_external_handoff.md` を参照する。
未統合の実装はそのブランチで比較・復元できる。

## 採用した拡張方針 — 2026-09-11

ユーザー指定により、**既存JMLE・MMLの高速化を優先**する。
JAXは同じモデル・推定対象を保つ任意の計算基盤として評価し、NumPyroは後続の
ベイズ推定拡張とする。ConQuestとTAMは、独立した照合と既存分析手順への受け渡しを担う。
具体的な段階、ローカル環境の確認結果、数値・性能の検証条件は
[JAX・NumPyro・ConQuest・TAMの拡張計画](jax_numpyro_conquest_tam_plan.md)に従う。
これは実装済み機能の追加ではなく、今後の開発方針の追加である。

2026-09-13に、Stan/NumPyroの**オフライン実行コードの選択式配布**を具体化した。
現存Stanコードの同定・閾値制約・対応外設定と、旧Posterior形式をB0で監査する。
MCMC/PPCの実装は既存G4以降の条件を守り、ダウンロードと結果読込の資格を別に判定する。
mfrmr `development/` の未コミット変更を含む0.2.4.9000を読み取り、対象別の根拠、
高次数GH微小重みの未解決事項、固定/再推定した不確実性を拡張計画に対応づけた。
次のUXは[U0/U1の既存操作の改善](ux_adversarial_audit.md#next-ui-refinement--2026-09-13-planned)から開始し、
PCA・バイアス・Report & Exportの安定した配置を維持する。新しいエンジンの入口を
初回分析に追加しない。今回は文書の更新であり、コード配布やUI改善の実装完了ではない。

## UXの改善 — 2026-09-11

統合開発ブランチで、開始画面と手元データの入力を改善した。
目的を説明する日英の見出し、折りたたんだ版情報、開始前の進捗表示の除去、
中央の貼り付け・アップロード欄、重複注意の整理を反映した。
既存結果がある再描画では入力欄を折りたたむ。入力形式変換とエラーは中央、
ヘルプ表示中は入力状態を保つためサイドバーに描画する。
データ不足時にも出ていた無条件の「Ready for estimation」と古い実行場所案内を削除した。

関連68テストと起動診断を通過。最後の案内削除後に、手元データ経路と画面の
段階的表示を再確認した。Chromeで日英の開始画面、合成した5行の貼り付けから
列対応・データ不足の注意までを確認した。パッケージ追加は不要だった。
これは段階3の研究計画画面の統合や、正式なブラウザ受入の完了ではない。

続いて結果への導線を点検した。解釈の判定と次の操作は既に実装されているが、
横長の表では限界や次の操作が画面外に隠れ、現在の焦点を示す表も重複していた。
既存の判定表を使って「解釈状態・主な根拠・まだ結論にしないこと・次にできること」
を縦に読める表示へ変更した。焦点の詳細表は補助詳細へ、状態別件数は初期確認の
凡例へ移し、案内の描画を共通化した。推定・判定・出力データの変更はない。
関連115テストを通過し、5種類の状態×日英で説明の保持、推奨先への移動、
実行結果の保持を確認した。

### 全体を点検するときの判断基準

中心の目的は、利用者が評定データを分析し、根拠と限界を理解して利用できる
一つのアプリに統合すること。各変更は、解消する利用上の問題、既存機能での対応、
統計的な根拠への影響、確認できた効果を照合して採否を決める。
画面テストの成功を統計的な資格や利用者テストの成功に読み替えない。

数学的正確性を最優先する追加方針に従い、既知の推定・不確実性の問題を
段階2の性能改善より先に点検する。公開版改善は数学的な基準を確認して取り込む。
入力・結果に残る英語や専門用語は
各導線を接続するときに整理する。研究計画は分析前の入口へ、ConQuest/TAMは
比較・受け渡しの画面へ接続する。JAXの実装選択を初回操作の必須判断にしない。

## 統計・検証の現在地

- 出力保護 G0 と同定座標 G1 は完了記録があり、G2 以降の資格を統合で解除しない。
- exact CMLE は研究用のまま。事前設計モジュールの追加で公開セレクターに出さない。
- `kac200` は200独立Personベクトル・600データセットの確認試験まで完了。
  v1登録基準では方向と精度の判定を満たしたが、追加監査では Python MML の
  最適化停留性の資格が未完了とされた。過去の結果をv2で置換しない。
- 自由分散MML v2 は開発段階。独立した新規データでの資格検証が必要。
- 自由分散MMLの旧「Profile SE」は他パラメータ固定の曲率だったため、
  SE・信頼区間の生成と推論用表示を保留した。古い保存結果にも出力保護を適用。
  [数学的根拠と修正範囲](mml_population_sd_uncertainty_audit.md)を参照。
- 続くRSM／PCMのQ31・Q61開発確認で、EMの停止と共同停留性の差を再現した。
  EMの成功だけで自由分散MMLに `InferenceReady=True` を付ける経路を修正し、
  結果案内・出力・感度分析の開始にも保留を適用。独立した資格検証と被覆率は未完了。
- 実ブラウザのアクセシビリティ受入は未完了。
- 2026-09-12に、既存24 Person例の自由SD RSM/PCMを独立R計算と照合した。
  同じ点での32比較は尤度・勾配・EAP等の実装基準を通過。一方、R初回再最適化8件中
  2件の異常終了は保持し、別方式の再探索と数値差を分けて記録した。
  連続積分との比較では、Q31の停留性を満たしてもEAP・事後SDに求積誤差が残った。
  [実行記録と限界](mml_cross_language_development_check.md)を参照。推論資格は未付与。
- 同日の追加確認では、独立R解析勾配で32点を照合し、同じ8条件の再推定は正常終了した。
  目的関数変化による停止と勾配基準の達成は区別し、初回2件の異常終了記録も保持する。
  SDに伴う求積点移動の微分と、EMの分散モーメント条件が有限次数では異なることを記録した。
  [数式・追加診断・再現手順](mml_cross_language_development_check.md#追加確認解析勾配と有限求積の停止条件)
  を参照。このR単独確認の時点ではPythonへの接続は未着手だった。
- 続いて、Pythonの開発用stationarity・quadratureアダプターに解析SD勾配を接続した。
  既存の残差・事後重みを再利用し、勾配計算方式を実行記録へ追加。
  独立R解析勾配の32点との最大差は約7.5e−14。重み付き評定・母集団回帰も勾配照合した。
  新しいRSM・PCM開発データの12実行は正常終了し、関連111テストが通過。
  ただし極端反応を含むPCMのSDはQ31/61/121で約1.61/2.14/1.64と変動した。
  次は連続積分参照と次数間の推定点交換により、求積誤差と局所解の影響を切り分ける。
  [Python接続の範囲と新データ検証](mml_cross_language_development_check.md#python開発核への解析sd勾配の接続)
  を参照。標準EMの推定経路と推論資格の保留は継続する。
- mfrmr 0.2.4.9000・TAM・ConQuestのMML求積をソースと保存記録で照合した。
  RSM/PCMの既存ストレス比較は、推定の終了と求積精度を分ける必要を裏付ける。
  Python/mfrmrのGH関数を実行照合し、mfrmr高次数の微小重みゼロと保存校正の正重み要件の
  不整合も記録した。[求積レビュー](mml_quadrature_cross_engine_review.md)を参照。
  新しい外部モデル推定・mfrmrの変更・Pythonの資格昇格は行っていない。
- 2026-09-13、前記PCMストレス例の3保存点をQ31/61/121/181へ交換して再探索した。
  12条件すべてが正常終了し、同じ次数ではほぼ同じ解に到達した。一方、独立R適応積分との
  同一点比較では、Q181でもEAP最大約0.00117・事後SD約0.00207 logitの差が残った。
  関連30テストが通過。次は連続積分目的関数での再推定と停留性確認。
  [PCM追加診断](mml_cross_language_development_check.md#pcm追加診断求積誤差と出発点への依存)を参照。
  既定求積次数・標準EM・推論資格は変更していない。
- 同日、PCMの4 GH終点を連続積分の目的関数で再推定し、全開始点からSD約1.6541712へ到達。
  独立R・全座標勾配・局所Hessianの検査を通過した。実機TAMは±10・201点で参照解との
  最大座標差約3.72e−7となったが、3条件とも2,000反復上限なので正常収束とは扱わない。
  ConQuestはmacOSの付属ライブラリ読込み拒否によりモデル推定前に終了した。
  次はTAMの更新量・停止条件と範囲/密度の分離診断、ConQuest実行環境の復旧後の照合。
  [連続積分再推定・TAM実機照合・ConQuest起動記録](mml_cross_language_development_check.md#pcm連続積分での再推定とtam実機照合)
  に詳細を保持。既存の推論資格保留は継続する。
- 続いてTAMの更新量を追跡し、項目座標の更新量が停止許容誤差を上回ることを特定した。
  別の停止設定による範囲×密度の9条件は正常終了。最終座標と保存EAPの微小な時点差、
  平均固定時の分散中心化の順序も確認した。厳しいEAP再現検査の6件の不通過は保持した。
  ユーザーの通常Terminal経路ではConQuest 5.47.5 Standard Versionの7条件が実行できた。
  固定グリッドの推定値はTAMと近いが、GH低次数・保存尤度と更新座標の時点差・別MCによる得点計算を
  分けて扱う必要がある。[更新監査とConQuest実測](mml_cross_language_development_check.md#tam停止条件の追跡とconquest実測による追加確認)
  を参照。次は同一校正での得点MC精度、GHの記録順序・停止条件・開始点依存を検証する。
- 2026-09-14、ConQuestの校正を固定した得点計算31条件を完了した。全条件の座標・平均・
  分散・A行列を照合し、同じ抽出数とseedの再実行は保存得点が完全一致した。
  10 seedを集計すると、`p_nodes` 2,000→200,000でEAPのRMSEは0.014242→0.000857、
  事後SDは0.007443→0.000428 logitへ減少。残る最大差は0.003964・0.001597 logitであり、
  一般的な必要抽出数や推論資格は確定しない。初回の平均アンカー番号0の読込み失敗を保存し、
  実機が受け付けた番号1で別実行した。全31件に残る「推定パラメータ数−1」は未解決として
  記録し、ネイティブ情報量基準を使用しない。
  [得点MCの条件・結果・次工程](mml_cross_language_development_check.md#conquest校正固定での得点mc精度)
  に、GH求積の診断と分けて記録した。
- 同日の2条件対照では、CASES制約を残して明示的な平均アンカーだけを外すと、パラメータ数が
  −1から0に戻った。全校正・deviance・得点は一致し、この設定における表示問題を切り分けた。
  GHの1,543履歴行も再評価し、報告尤度と座標の1行の時点差を確認した。最終更新はほぼ0でも、
  有限GHのlog(SD)勾配はQ31/61/121/181で約1.827/−3.549/0.374/0.03685残った。
  全1,539組の前後反復では、直前の事後平均で中心化する式から次の分散を最大1.111e−6の差で
  再現した。これは保存結果の事後的な数値再構成であり、内部実装の証明ではない。
  [アンカー対照とGH全履歴監査](mml_cross_language_development_check.md#conquest平均アンカー対照とgh全履歴監査)
  に数式・精度検査・次の対照条件を記録した。推論資格は継続保留。
- 同じ日の直接GH解からの8実行で、開始時に有限GH勾配が十分小さくても、ConQuestの最初の
  更新がその解から離れることを確認した。初回分散は中心化式の予測と最大3.423e−7の差で一致。
  31・61点は開始値によって最終座標が大きく変わり、181点は最終SDが前回と一致したが、
  log(SD)勾配約0.03681と得点の求積誤差が残った。4開始点・12更新後座標は独立Rと照合済み。
  [直接GH解からの開始値対照](mml_cross_language_development_check.md#conquest直接gh解からの開始値対照)
  に停止理由・返却点・実行最終点・連続積分との差を分けて記録した。
- 構造更新の19箇所を追加照合し、古い事後重み・分散変更後の求積点・ネイティブ順1巡の
  座標更新で、保存値を最大1.946e−6の差まで再現した。停止直前では他の仮説も近くなるため、
  初回など移動の大きい点を主な識別根拠とした。GH61の4実行では、`keeplastests`の変更は
  履歴を変えず返却点を変え、短い反復上限では初期尤度より悪い点も返された。
  [構造更新と返却点の対照](mml_cross_language_development_check.md#conquest構造更新と返却点の対照)
  に検査と限界を記録。次の主工程を、共通尤度・連続積分を基準にした独立条件の検証とする。
- 独立した通常／広い裾のPCMデータを各2組生成し、事前固定の仕様で連続積分とGH4次数を
  比較した。4組ともR・アプリとの算術照合、連続参照、最終勾配の検査を通過した。
  通常条件はGH121で得点精度の目安を満たしたが、広い裾はGH181・固定グリッド±12でも
  不足した。GH31の1組では開始値による別解も保存した。
  [独立条件の記録](mml_cross_language_development_check.md#独立した通常条件と広い裾での求積検証)
  に基づき、次工程を範囲×密度の別仕様での検査と同じデータのTAM/ConQuest実行に具体化した。
  4組の開発検査はSE/CI/被覆率の資格検証ではなく、既定次数・推論保護は変更していない。
- 範囲×密度の24条件と、同じ開始値のTAM・ConQuest各8件を検証した。
  ±12の広い裾では間隔半減だけで精度不足は解消せず、±16で事前の得点目安を満たした。
  独立Rの算術照合は通過。ConQuestとTAMの自由座標差は最大3.992e−5だった。
  一方、Pythonの96最適化中1再始動がABNORMALのため、この段階の総合フラグはfalseを保持した。
  TAMの履歴取得エラーも失敗記録を保全し、推定設定を変えず取得処理を修正した。
  [実測結果と未解決事項](mml_cross_language_development_check.md#積分範囲と密度の対照およびtamconquestの実測)
  に従い、次は終了状態と数値精度を区別する契約を整理する。
- 保持したABNORMALを同設定で再現し、直線探索50回での上限到達を記録した。
  独立60／90桁計算で微小な改善が倍精度では悪化へ反転することを確認した。
  元の失敗・総合falseを保持し、終了状態・勾配／曲率・積分精度・得点法の
  [判定契約](mml_cross_language_development_check.md#異常終了の再現と数値精度の判定契約)
  を既存モジュールに沿って具体化した。関連34テストと診断の算術検査が通過した。
  次は尤度差そのものの安定計算を別仕様で検証し、閾値緩和で今回の失敗を消さない。
- 尤度差の安定計算を別仕様で検証し、4基準点・22比較の高精度照合が通過した。
  元の微小な改善2件は絶対誤差2.65e−22未満で計算できた。非停留点・SD変化も含む。
  [数式・結果・次工程](mml_cross_language_development_check.md#尤度差の安定計算と高精度照合)
  に従い、次は目的関数の基準変更と相対停止条件を切り分けた最適化対照を行う。
  今回は開発用の算術検証のみで、推定核・UI・元の失敗・推論資格は変更していない。
- 固定基準点の最適化対照96回を記録した。近い保存済み解を基準にした`ftol=0`の
  8系列は全て要求勾配1e−8を満たしたが、他条件の未達・6回のABNORMALは保持した。
  [結果と監査](mml_cross_language_development_check.md#固定基準点を用いた最適化対照)
  は最適化と積分精度の違いも示す。次は当該実行の予備推定から固定基準点を作る
  2段階手順を別仕様で検証する。推定核・推論資格を昇格させる結果とは扱わない。
- 生成前に仕様を固定した新しい80人データ4組で、自己の予備推定を基準にする2段階手順を検証した。
  24系列全てが精密化の検査を通過し、独立Rと60／90桁照合も通過した。
  [新データの結果と境界](mml_cross_language_development_check.md#保存済み解を使わない二段階検証)
  に、広い裾の積分精度不足と、既存APIが`ftol=0`を受け付けない点を記録した。
  次は動くGH点の尤度差を数学的に確認してから、段階別記録・停止条件を既存APIへ接続する。
- SDによるGH点の移動を含む尤度差を別ソースで実装し、2データ・4次数・48比較で
  独立R・高精度計算・全24座標の勾配照合が通過した。
  [動くGH点の数式と結果](mml_cross_language_development_check.md#sdとともに動くgh点の尤度差)
  に、固定点への誤置換を検出した対照と、広い裾でGH181にも残る積分誤差を記録した。
  次は既存の2段階検証手順をGHへ接続し、開始値依存と求積誤差を分けて評価する。
  今回は新規最適化・アプリ接続・TAM/ConQuestのネイティブ再実行は行っていない。
- 動くGH点で2段階推定を実行し、4データ・3開始SD・4次数の48系列が精密化の検査を通過した。
  同じ次数の開始点間差は最大8.181e−10だが、広い裾では推定SDが次数によって非単調に変化し、
  181点でも積分誤差が残った。[結果と保存記録](mml_cross_language_development_check.md#ghでの二段階推定と次数依存)
  に、192呼出しの終了状態・独立R・高精度照合を記録した。
  次は旧ソースを保全して段階別の基準点・生のNLL・終了状態を既存APIへ接続し、
  既存の求積感度検査のNLL再構成を維持する。アプリ既定値・推論資格は変更していない。
- 任意利用の2段階APIを接続し、生NLL・基準点からの尤度差・予備推定と精密化の終了状態を
  分けて保存した。8系列は前回解と一致し、求積感度の既存判定も通常通過／広い裾未達を保った。
  [API検証と旧版ソースの保全](mml_cross_language_development_check.md#二段階推定apiへの接続と旧証拠の保全)
  に、関連83テスト、高精度照合、旧記録のハッシュを変更しない保全方法を記録した。
  次はアプリの観測デザインを使う差分関数を開発用アダプターへ接続する。
  現時点のPCM数式検証は完全交差・重み1に限定され、既定の推定画面への接続は行っていない。
- 観測行を使う尤度差と2段階呼出しを開発用アダプターへ接続した。RSM／PCMの欠測、
  ゼロ・分数重み、正負の符号、非ゼロ固定値・群平均、受験者回帰平均を含む算術検査と、
  既存RSM結果からの4回の最適化が通過した。関連87テストも通過した。
  [観測行の検証記録](mml_cross_language_development_check.md#観測行を使う差分関数と開発用アダプター)
  に、60／90桁の独立式との比較と、次の欠測・重み付きPCMの再推定／積分精度検証を記録した。
  既定推定への接続や、重み付き推論・SE／CI／被覆率の資格付与は行っていない。
- 同じ欠測・重み付きPCMを3開始SD×4次数で再推定し、12系列全てが有限GH停留性を通過した。
  [再推定と連続積分照合](mml_cross_language_development_check.md#欠測重み付きpcmの再推定と連続積分照合)
  に、48回のPCM最適化、144点の目的関数再計算、独立算術・60／90桁比較を保存した。
  GH31／61／121は積分精度が未達、GH181は今回の得点目安を満たした。Q121／Q181の既存比較は
  構造座標差で未達を維持する。次は連続積分の最適解との差を確認する。既定次数・公開推定・推論資格の変更はない。
- 4つのGH返却点から連続積分を再推定し、同じ正曲率の局所解（SD約2.27265279973）を得た。
  [連続局所解の照合](mml_cross_language_development_check.md#欠測重み付きpcmの連続積分による局所解)
  に、8回の生NLL最小化・8回の勾配精密化、48評価点の再計算、独立R照合を記録した。
  GH181は再校正を含む得点差も目安以内だったが、GH121の総EAP差は約5.55e−4で未達だった。
  次は独立データの欠測・重み条件を事前固定して、校正値と得点の総差を検証する。公開推定や推論資格は変更していない。
- 新しい32人データ2組で、完全・欠測・同じ欠測への回答重みの3デザインを対照した。
  24 GH系列は全て停留性を満たし、同一次数の開始点間差は最大1.31e−8だった。
  [独立した観測デザインの検証](mml_cross_language_development_check.md#独立データにおける欠測と回答重みの対照)
  に、96 GH呼出し・12連続局所参照・独立R・60／90桁照合と、432評価点の再計算を記録した。
  通常3条件はGH121／181で総合目安を満たしたが、広い裾3条件はGH181でも未達だった。
  次は同じデータの重み1の完全・欠測条件からTAM／ConQuest比較を準備し、共通座標での尤度・
  得点を揃えてから再推定を比較する。今回の新規ネイティブ推定や既定推定・推論資格の変更はない。
- 同じ4つの重み1条件でTAM／ConQuestの固定対照16件・再推定16件を実行した。
  [回帰・欠測を揃えた実機比較](mml_cross_language_development_check.md#独立した重み1条件のtamとconquest実機比較)
  に、識別制約の変換、受験者別の有限事前正規化、独立Rと保存記録の監査を記録した。
  自由座標のソフト間差は最大1.761e−5だが、広い裾の±12では両者とも連続参照への精度が不足した。
  ±20は今回の総合目安を満たす。ConQuest標準EAPと校正値からの再計算は区別し、
  次に標準得点の点数・乱数による変動を検証する。終了理由・準備段階の失敗を保持し、推論資格は変更していない。
- 続いて4条件の校正を固定したConQuest標準得点124回を実行した。
  [点数・乱数の対照と保存記録の監査](mml_cross_language_development_check.md#回帰欠測条件でのconquest標準得点の乱数変動)
  に、3点数×10 seedと4反復対照、連続R参照との誤差を記録した。
  校正・尤度履歴は不変で、反復は前回の標準得点も完全に再現した。点数100倍でRMSEは縮小したが、
  200,000点でもEAP最大差は約0.00944 logit残った。1,635成果物・23ソースの再監査が通過した。
  次は非単位回答重みの定義を揃える。標準得点の一律の精度保証や、既定推定・推論資格の変更は行わない。
- [回答重みと受験者重みの固定値対照](mml_cross_language_development_check.md#回答重みと受験者重みの区別および固定値対照)
  で24回を実行した。TAMの平均1の受験者重み付きNLLは一致したが、ConQuestの重み付き表示NLLの
  事前2仮説は非単位重み8件で不成立だった。事後診断では十分統計量が平均1の重みを反映し、
  表示NLLは受験者重みなしの式に一致した。失敗・旧ソースを保全し、252成果物・29ソースを再監査した。
  mfrmr開発ソース0.2.4.9000の回答重みはPythonと同じ積分内のべきだった。新たなmfrmr推定はない。
  次はConQuestの自由パラメータを限定して更新と表示値を対照する。非単位回答重みの互換性や推論資格は未認定。
- [受験者重み付きの1変数対照](mml_cross_language_development_check.md#受験者重み付きの1パラメータ更新と返却点)
  としてConQuest36回を実行した。初回更新は重み付きEMと一致し、最後の返却値は独立した停留点に
  出力精度内で一致した。既定返却は非単位重み8件とも4反復目で、停留点との差が最大0.01063 logit残った。
  事後診断で400履歴行のNLLと更新前座標の対応、99更新のEM再現を確認した。
  547実行成果物・26ソースを再監査し、追加診断の554成果物を保全・照合した。
  次は過去の履歴解釈を同様に各ログから点検する。アプリ既定・推論資格・mfrmrの対応状況は変更していない。
- [過去のConQuest履歴の再点検](mml_cross_language_development_check.md#過去のconquest履歴の時点と丸め幅の再点検)
  でGH16履歴と固定グリッド8履歴を再評価した。比較可能な5,139行すべてで更新前座標と表示NLLが
  出力丸めの範囲内で整合し、528行は更新後座標では説明できなかった。残りは時点を一意に識別できない。
  表示値の時点差と、返却座標に残る勾配・積分誤差を区別する。432旧ファイルと25新成果物を照合した。
  次は保存済みの回帰・欠測8再推定で受験者別の事前平均を含めて点検し、比較レポートの評価量を整理する。
- [回帰・欠測履歴と返却点の勾配](mml_cross_language_development_check.md#回帰欠測履歴の時点と返却点の勾配)
  を8件で再計算した。全1,335行で更新前座標と表示NLLが丸め幅内で整合し、289行は更新後では説明できない。
  広い裾の±12では、返却点に正規化済み・未正規化の両方の有限尤度勾配が残った。表示一致と停留性を分ける。
  348旧成果物・9新成果物・24ソースを照合した。比較レポートで先に示す4つの問いと、詳細に残す評価量を定めた。
- [4つの問いを分ける比較レポート](mml_cross_language_development_check.md#4つの問いを分けた比較レポートの実装)
  を実装し、保存済み8件のHTML・CSV・JSON・ZIPを生成した。最初に4結果を示し、数値・評価点・根拠は折りたたむ。
  校正値の同等性と標準得点の安定性を未評価のまま保持し、広い裾の±12の未達も保存した。
  10テスト、8レポートの完全再構成、ZIP34項目の一致、ブラウザ表示を確認した。
  Streamlitへの接続と既存の外部比較テンプレートの説明更新は次段階。現在の分析の推論資格へは接続していない。
- [Streamlitへの比較準備の接続](mml_cross_language_development_check.md#streamlitへの比較準備の接続)
  を行い、現在の分析にひも付いた未評価のレポートと入力一式を、必要なときだけ表示・保存する形にした。
  Rのひな形はモデルの対応付け前に停止し、順位相関だけで一致を判定する案内を廃止した。
  外部比較結果の取込みは未実装。既存の数値検証は変更前の実行コードを保存し、8件を完全再構成できた。
  推定・求積既定値・推論資格・公開版は変更していない。
- [限定PCMランナー](mml_cross_language_development_check.md#限定pcmランナーと実行確認)を追加した。
  対応する固定SD・単位重みPCMに限ってスクリプトを同梱し、両ソフトの固定値検査後にだけ再推定する。
  12回の外部実行を確認し、校正値差と得点のMC差を分離して保存した。
  併せて、EMの相対尤度停止後も同じQで勾配が残る例と、同一点のQによるNLL変動を記録した。
  次の優先課題は、固定SDでの停留性確認と求積感度の切り分けである。
- [固定SDの停止とGH次数感度](mml_cross_language_development_check.md#固定sdのem停止とgh次数感度の切り分け)
  を同じ2例で分離した。36回の最適化と108評価点の再計算を保存し、全12終点で有限GH勾配は9.22e−10未満だった。
  GH31の仕上げは自身の尤度を改善する一方、連続NLLをわずかに悪化させた。GH121では連続NLL差は最大4.15e−9、
  保存済みTAM／ConQuestとの校正値差は最大3.05e−8／1.29e−6 logitだった。
  次は任意の固定SD仕上げ処理と返却点の診断を共通の尤度計算へ接続する。公開推定処理・既定Q・推論資格は未変更。
- [固定SD仕上げの開発用API](mml_cross_language_development_check.md#固定sd仕上げ候補を返す開発用api)
  を接続した。無効時は追加処理をせず、有効時も元の結果を保持して候補座標・ラベル付き得点・診断を別に返す。
  関連80テストと、最終ラベル処理を含む5テストが通過した。前回の2終点を最大3.65e−15 logit差で再現した。
  追加のSD1.6の例では、GH121が停留しても連続NLL差0.00704、事後SD差0.000775が残ることを記録した。
  次は既存の数値詳細画面に返却点の診断を簡潔に示す。候補を全結果へ適用する処理と公開経路への有効化は未実装。
- [返却点診断の画面接続](mml_cross_language_development_check.md#返却点診断のコンパクトな画面接続)
  を追加した。固定SD MMLの初期表示を短い案内と閉じた数値詳細にまとめ、対応するPCMではボタン操作で
  同じ求積則の返却点だけを再評価する。停止条件・勾配・求積精度を分け、入力変更時は前の確認結果を解除する。
  関連168テスト、日英・モバイル幅の部品表示、既存比較出力110ファイルの内容一致を確認した。
  次は診断の保存と既存の求積感度確認の接続である。推定値の差替え・既定次数・推論資格・公開版は未変更。

詳細は [MML v2](../validation/MML_FREE_SD_STATIONARITY_V2_ROADMAP_20260811.md)、
[統計エンジンの修正計画](statistical_engine_remediation_roadmap.html)、
[ブラウザ受入](browser_accessibility_acceptance.md) に従う。
過去の README / ROADMAP にある「次の確認試験」という記述より、対応する
最新の実行・監査記録を優先する。本台帳は開発統合状況の入口であり、統計ゲートの代替ではない。

## 再確認用コマンド

```bash
git branch --show-current
git log -1 --oneline main
git diff --stat archive/public-main-20260911 integration/unified-app
git diff --stat archive/working-20260911 integration/unified-app
python3 -B -m pytest -q -p no:cacheprovider \
  tests/test_design_spec_cost.py tests/test_rating_design_assignments.py \
  tests/test_rating_design_topology.py tests/test_design_planning.py \
  tests/test_simulation_conditions.py tests/test_structural_transition_preflight.py
python3 streamlit_app.py --doctor
```

既存の研究テストにはローカルに保全した `validation/` の結果を読むものがある。
source snapshot だけの新規 checkout ですべての研究テストが通るとは主張しない。
必要な凍結fixtureの選別・配布・復元は段階6で扱う。

## 公開準備 — 2026-09-13 / 0.2.17-beta

ユーザーのコミット・プッシュ・デプロイ指示により、確認済みの統合アプリ
`ba81fcf04` を公開内容の基準とした。公開版 `d8558fc6b` は履歴上の親として
保存し、強制プッシュは使用しない。これは公開版の全変更を移植したという
意味ではなく、段階2の残りの性能改善は引き続き未完了である。
版番号を `0.2.17-beta` に更新し、独立した一時チェックアウトで公開ゲートを
実行する。元ディレクトリの検証結果・退避資料は変更も公開もしない。
自由SD MMLの推論資格、外部エンジンとの同等性、正式なブラウザ受入は
今回の公開によって昇格させない。

公開完了記録: [PR #3](https://github.com/Ryuya-dot-com/MFRM_Python_Application/pull/3)を
`530b92468f8b7c5f1b55b4db15c7a4b9b6619696` としてマージした。
公開treeは検証候補 `4bf96d1ab` と一致し、local mainとintegrationブランチも同期した。
[候補CI](https://github.com/Ryuya-dot-com/MFRM_Python_Application/actions/runs/34748455714)と
[マージ後CI](https://github.com/Ryuya-dot-com/MFRM_Python_Application/actions/runs/34749786157)は
Python 3.11/3.12とも全工程成功。候補CIの各環境では56自己検証、72 UX契約、
通常suite 2,126 passed / 2 skipped / 84 deselected、benchmarkとdemo exportを確認した。
84件の除外は既存の互換62件とローカル保管証拠replay22件であり、新しい統計資格の根拠に含めない。
Streamlit管理画面でmain / streamlit_app.pyを確認し、旧版が残ったためRebootを実行。
公開画面で0.2.17-betaとsource commit `530b924`、サンプル準備画面の起動を確認した。
ホストのログはPython 3.13.15 / Streamlit 1.63.0だった。

2026-09-13の公開後のローカル変更として、MITの画面案内、Ryuya Komuro名義の
APA/BibTeX/CFF引用情報、状態を保持するHomeと分析への復帰を追加した。
詳しくは[UX実装記録](ux_adversarial_audit.md#license-software-citation-and-home--2026-09-13-local-implementation)。
これは公開済み `530b92468` の機能ではなく、次回公開に向けた未コミット変更である。
推定コード・数理資格・既存ライセンス本文は変更していない。

同日の継続作業で、保存結果の版と照合したソフトウェア引用を論文テンプレート、
Word/PDF/HTML報告書、標準結果ZIP・論文binderへ接続した。旧版・版未記録の結果を
現在版の引用で補完せず、確認案内を残す。関連78テスト、続く49テスト
（引用テストの再実行を含む）が通過。Word出力2種の全17ページとPDFの引用欄を
描画確認した。詳細と検証範囲は[UX実装記録](ux_adversarial_audit.md#result-bound-citation-exports--2026-09-13-local-follow-up)に記載。
これも未コミット・未公開の報告支援変更であり、新しい統計的資格を意味しない。

## 2026-09-15: 0.2.18-beta 公開準備

日本語の案内、ホームへの移動、MIT・引用情報、比較用の書き出し、
固定SD PCMの推定点の確認を今回の公開対象とする。通常の推定器の既定動作と、
自由SD MMLの標準誤差・推論を保留する条件は維持する。

公開前の92ファイルとリリース設定は、ローカルの
`validation/generated/release_v0_2_18_beta_20260915/pre_release_sources.zip` と
SHA-256 manifestへ保存した。過去の数値検証を新しいバージョンでの実行結果と
読み替えず、保存済みの元ソースと結果を保持する。研究結果を含まない別の
チェックアウトで公開前の検証を行い、GitHub CIの通過後に公開mainへ統合する。
