# MML求積の比較：Python・mfrmr・TAM・ConQuest

追記（2026-09-15、返却点診断の画面接続）: 固定SD MMLの計算確認を短い案内と閉じた数値詳細にまとめた。
対応するPCMでは、ボタンを押したときだけ同じGH則の返却NLLと勾配を再評価する。
停止条件の達成、勾配の小ささ、求積精度を別々に示し、求積精度は未確認のまま保持する。
[対応範囲・入力照合・UI検証](mml_cross_language_development_check.md#返却点診断のコンパクトな画面接続)を参照。
この接続では再推定・TAM／ConQuestの実行・候補の適用を行わず、新しい一致性の証拠は追加していない。

追記（2026-09-15、固定SDの開発用API）: 同じGH則での仕上げ候補と返却点診断を、アプリの尤度計算へ接続した。
元の分析結果を変更せず、候補のEAP・事後SDを元のラベルとともに別に返す。
前回の2例を再現した一方、追加した周期的回答のSD1.6の例では、GH121の有限勾配が4.89e−10でも、
連続NLL差0.00704、事後SD差0.000775が残った。EAP差は約3e−15であり、平均得点の一致だけでは積分精度を判断できない。
[APIの範囲・テスト・追加例](mml_cross_language_development_check.md#固定sd仕上げ候補を返す開発用api)を参照。
Mステップの実効`ftol`は1e−9である。前節の説明は呼出し値1e−7と区別するよう補正した。

追記（2026-09-15、固定SDの停止と求積の分離）: 同じ2例でGH31／61／121を各2開始値から仕上げ、
有限GHの勾配と独立した連続積分を比較した。全12終点の有限GH勾配は9.22e−10未満だったが、
GH31の仕上げは連続NLLをわずかに悪化させた。小さい停止誤差と求積誤差が打ち消し合う場合がある。
GH121では連続NLL差は最大4.15e−9、保存済みTAM／ConQuest校正値との差は最大3.05e−8／1.29e−6 logitだった。
連続勾配は約3e−8残り、有限GHでの停留性の判定をそのまま連続尤度へ移すことはできない。
[設計・次数別結果・次の実装範囲](mml_cross_language_development_check.md#固定sdのem停止とgh次数感度の切り分け)を参照。
新しいネイティブ実行、既定次数の認定、MC得点の追加検証は行っていない。

追記（2026-09-15、限定PCMランナー）: 単位重み・固定SDなどの対応条件を満たす場合に、
TAM／ConQuest用のオフライン実行スクリプトを比較ZIPへ同梱するようにした。
固定値での両ソフトの対応検査を通過してから再推定する。12回の外部実行を確認し、
完全データ・1回答欠測の校正値差は最大7.67e−7／1.26e−6 logitだった。
一方、今回のアプリEM返却点は「相対尤度で収束」でも、同じGH31の勾配最大値が0.00916／0.01023残った。
同じ座標のGH31と401点固定グリッドの合計NLL差は0.00367／0.00428だった。
[条件・数値・保留事項](mml_cross_language_development_check.md#限定pcmランナーと実行確認)を参照。
次は同じQでの停留性と、求積による変動を分けて精緻化する。標準得点のMC安定性や推論資格は認定しない。

追記（2026-09-15、Streamlitへの接続）: 「レポートと出力 → 確認事項 → 外部ソフトと比較する」から、
現在の分析の比較準備ZIPを取得できるようにした。4項目は未評価から始まり、開発時の8件を流用しない。
設定・回答重み・アンカー・回帰デザイン・返却点を省略せず記録し、内容ハッシュで結び付ける。
順位相関だけの判定案内と丸め平均による自動比較を廃止した。Rは対応付け用のひな形で、推定前に停止する。
[接続範囲と確認結果](mml_cross_language_development_check.md#streamlitへの比較準備の接続)を参照。
実測結果の取込みやTAM／ConQuest／mfrmrへの対応済みランナーは次段階。推定法・求積既定値・推論資格は変更していない。

追記（2026-09-14、比較レポートの実装）: 校正値・停留性・積分精度・標準得点の安定性を独立に記録する
処理を実装し、保存済み8件のHTML／CSV／JSON／ZIPを生成した。広い裾の±12の2件は未達を保持し、
校正の同等性と標準得点の安定性は全8件で判定保留とした。許容幅や検査がない項目を通過へ補わない。
[レポート一覧](../validation/generated/mml_comparison_reports_20260914/index.html)と
[判定仕様・検査・残る接続作業](mml_cross_language_development_check.md#4つの問いを分けた比較レポートの実装)
を参照。既存Streamlit画面への接続は次段階であり、現在の分析に外部比較済みの表示を追加していない。

追記（2026-09-14、回帰・欠測履歴）: 保存済み8再推定の全1,335行で、表示NLLは回帰係数・分散の
丸めを含めた更新前座標と整合し、289行は同じ行の更新後座標では説明できなかった。
一方、広い裾の±12条件では、返却座標の正規化済み有限グリッド勾配が最大0.1522／0.2892残った。
未正規化の式でも回帰係数の勾配が0.00568／0.00919あり、正規化の説明だけで停留性は認定できない。
[全行診断・勾配・比較レポートの要件](mml_cross_language_development_check.md#回帰欠測履歴の時点と返却点の勾配)
を参照。次は「校正値の近さ・停留性・積分精度・標準得点精度」を分けた比較結果の記録へ接続する。

追記（2026-09-14、過去の履歴と丸め幅の再点検）: GH16履歴と固定グリッド8履歴を再評価した。
比較可能な5,139行すべてで、表示NLLは更新前座標と出力丸めの範囲内で整合した。
528行では同じ行の更新後座標を丸め幅込みでも除外できたが、残りは時点を一意に識別できない。
以前の停留性・連続積分精度の結論は維持する。表示NLLと返却点のNLLの差だけを求積誤差としない。
[条件別結果・丸め幅の計算・解釈の補足](mml_cross_language_development_check.md#過去のconquest履歴の時点と丸め幅の再点検)
を参照。次は保存済みの回帰・欠測条件8再推定を、受験者別の事前平均の変化も含めて確認する。

追記（2026-09-14、受験者重み付きの1変数更新）: ConQuestの評価観点C1だけを自由にした36回を実行した。
重み付きEMの初回更新と最後の返却値は、独立した計算にCSV出力精度内で一致した。
ただし既定の`keeplastests=no`では非単位重み8件とも4反復目の座標が返り、独立した停留点から
0.00230〜0.01063 logit離れた。既定返却点は表示devianceの最小行でもなかった。
保存結果の事後診断では、400行の表示NLLが同じ行の更新後座標ではなく更新前座標に対応した。
[更新・履歴・返却点の全結果](mml_cross_language_development_check.md#受験者重み付きの1パラメータ更新と返却点)
を参照。次は過去の履歴解釈を各記録で再点検する。一般のMMLや既定設定全体の認証ではない。

追記（2026-09-14、回答重みと受験者重み）: 4条件×3受験者重み×2ソフトの固定値対照24回を実行した。
TAMの表示NLLは平均1へ正規化した受験者重み付きの式と一致した。一方、ConQuestについて事前に
用意した重み付き表示NLLの2仮説は非単位重み8件で不成立だった。保存結果の事後診断では、
十分統計量は平均1の重みを反映するが、表示NLLは重みなしの有限事前正規化済みの式に一致した。
[定義・失敗記録・診断の範囲](mml_cross_language_development_check.md#回答重みと受験者重みの区別および固定値対照)
を参照。表示値の一致から重み付き最適化の全体を認定せず、次に自由パラメータを限定した対照を行う。
Python／mfrmrの回答重みは積分内のべきであり、受験者重みへの置換は認めない。推論資格は変更していない。

追記（2026-09-14、回帰・欠測条件の標準得点MC）: 4条件で校正を固定し、`p_nodes`
2,000／20,000／200,000×10 seedと反復対照4件、計124回を実行した。
校正・データ・デザイン・尤度履歴は不変で、同じseedの得点は前回記録も含め完全に再現した。
点数を100倍にするとEAPのRMSEは条件別に約0.0110〜0.0349から約0.000830〜0.00244へ縮小したが、
200,000点でのEAP最大差は約0.00944 logit残った。
[全条件の結果と監査](mml_cross_language_development_check.md#回帰欠測条件でのconquest標準得点の乱数変動)
を参照。MMLの求積次数と得点計算の`p_nodes`を区別し、一律の十分な点数は認定しない。
次は非単位回答重みの尤度内での位置を揃えられるか確認する。既定次数・推論資格は変更していない。

追記（2026-09-14、回帰・欠測を揃えた実機比較）: 同じ4つの重み1デザインでTAM／ConQuestの
固定値対照16件と再推定16件を実行した。識別制約を変換し、受験者別の有限事前正規化を含めると
表示尤度は独立式と一致した。再推定の自由座標差は最大1.761e−5である。
ただし広い裾の±12は両ソフトとも精度不足で、±20では今回の4条件の総合目安を満たした。
[実機結果・標準EAPの区別・停止記録](mml_cross_language_development_check.md#独立した重み1条件のtamとconquest実機比較)
を参照。得点の一致は返却校正値からの決定的な再計算であり、ConQuest標準EAP出力の認証ではない。
この後の標準得点の点数・乱数対照は冒頭の追記を参照。推論資格や既定次数は変更しない。

追記（2026-09-14、独立データの欠測・重み対照）: 新しい32人データ2組をそれぞれ3観測デザインで
比較した。24系列は全て有限GH停留性を満たし、同一次数の開始点間差は最大1.31e−8だった。
通常の3条件はGH121／181で再校正を含む得点精度の目安を満たしたが、広い裾の3条件はGH181でも
未達だった。GH181の総EAP差は広い裾で約0.0114〜0.0303 logitである。
12連続局所参照・独立R・60／90桁照合と保存値の監査は通過した。
[条件別の結果と比較の優先順](mml_cross_language_development_check.md#独立データにおける欠測と回答重みの対照)
を参照。次は同じデータの重み1条件からTAM／ConQuest比較を準備する。
今回は新規ネイティブ推定ではなく、既定次数・推論資格・mfrmrの重みゼロ化の状況は変更していない。

追記（2026-09-14、欠測・重み付きPCMの連続解）: 4つのGH開始点から同じ連続積分の局所最小へ到達した。
計算上の最大勾配は2.11e−13、2刻み幅の曲率検査と独立R照合が通過した。
この再推定後の参照とのEAP最大差はGH181で5.36e−6、GH121で5.55e−4だった。
GH121は同じ校正値での積分誤差より総差が大きく、校正値の変化も精度判定に含める必要がある。
[連続局所解と得点の総差](mml_cross_language_development_check.md#欠測重み付きpcmの連続積分による局所解)
を参照。次は独立データで欠測・重みの条件を広げる。大域的最適性や一律の推奨次数を認証した結果ではない。

追記（2026-09-14、欠測・重み付きPCMの再推定）: 3開始SD×4次数の12系列は全て有限GHの
停留性検査を通過し、同一次数の開始点間差は最大7.43e−9だった。しかしGH31／61／121は
連続積分に対する得点精度が不足し、この事例ではGH181で目安を満たした。
Q121／Q181の比較は構造座標差約3.46e−4のため未達である。
[全系列・連続積分との比較](mml_cross_language_development_check.md#欠測重み付きpcmの再推定と連続積分照合)
を参照。GH181にも連続目的関数の勾配約2.47e−5が残るため、次は連続積分による最適解との比較を行う。
過去の広い裾の事例でGH181も不足した結果は維持し、一律の推奨次数へ読み替えない。

追記（2026-09-14、観測行への接続）: アプリの欠測・重み・制約を使う尤度差を開発用アダプターへ
接続した。RSM／PCMの60／90桁照合と勾配検査、関連87テストが通過した。
非ゼロの固定値・群平均・受験者の回帰平均と、正負のファセット符号を含む。
[観測行の検証範囲](mml_cross_language_development_check.md#観測行を使う差分関数と開発用アダプター)
を参照。次は、この重み付き・欠測PCMでの再推定と次数感度、返却点の連続積分照合である。
今回の照合は新たなTAM／ConQuestの実機比較や、高次数の求積重みゼロ化の解消ではない。

追記（2026-09-14、2段階API）: 予備推定と精密化を分け、生のNLLと基準点からの尤度差を
別に保持する任意利用のAPIを接続した。8系列が前回のGH解と最大6.677e−13の座標差で一致し、
独立R・高精度計算・関連83テストが通過した。既存の求積感度検査は通常条件を通過、
広い裾を未達と判定し、尤度差を生のNLLとして渡す誤りも拒否した。
[接続内容と旧証拠の保全](mml_cross_language_development_check.md#二段階推定apiへの接続と旧証拠の保全)
を参照。次はアプリの観測デザインから差分関数を構築する。既定推定への接続はまだ行っていない。

追記（2026-09-14、GHの2段階推定）: 4データ×3開始SD×4次数の48系列は全て精密化の
検査を通過し、同じ次数での座標差は最大8.181e−10だった。
一方、広い裾では次数による推定SDの変化は単調でなく、GH181にも事後SDの積分誤差
0.1046／0.0682が残った。[推定結果と限界](mml_cross_language_development_check.md#ghでの二段階推定と次数依存)
を参照。次は段階別の記録を既存APIへ接続し、求積感度の既存検査との整合を確認する。

追記（2026-09-14、動くGH点の尤度差）: SDによる点の移動を含む恒等式を実装し、
2データ×4次数×6座標の48比較で独立R・60／90桁計算・勾配の照合が通過した。
微小な尤度差の誤差は最大9.17e−20だった。同一座標での広い裾のGH181は、
連続積分とのEAP差0.0890・事後SD差0.1056が残る。
[数式・結果・適用範囲](mml_cross_language_development_check.md#sdとともに動くgh点の尤度差)
を参照。次はGHでの2段階最適化と開始点依存を検証する。今回の新規ネイティブ推定はない。

追記（2026-09-14、新データの2段階検証）: 生成前に仕様を固定した80人データ4組で、
予備推定から当該実行自身の基準点を作る24系列を検証した。全系列が精密化の検査を通過し、
3開始SD間の座標差は最大7.926e−10だった。独立R・高精度計算も通過した。
広い裾では±12の精度不足が新データでも残り、±20は今回の目安を満たした。
[条件と適用範囲](mml_cross_language_development_check.md#保存済み解を使わない二段階検証)
を参照。次はSDとともに動くGH点の尤度差を確認し、既存APIへの接続を検討する。

追記（2026-09-14、固定基準点での最適化）: 96回の対照を実行した。
近い保存済み解を固定基準にし`ftol=0`にした8系列は全て要求勾配1e−8を達成し、
開始点間の座標差は最大4.616e−9だった。他の条件の未達・6回のABNORMALは保持する。
広い裾の±12条件の積分誤差は残り、最適化の改善と求積精度を区別できた。
[全条件・高精度照合・次工程](mml_cross_language_development_check.md#固定基準点を用いた最適化対照)
を参照。次は保存済み解に依存しない2段階手順の検証である。

追記（2026-09-14、尤度差の安定計算）: 固定グリッドの確率比から尤度差を直接求める式を実装し、
非停留点を含む4基準点・22比較で60／90桁参照と照合した。
前回の微小な改善2件も符号を回復し、絶対誤差は2.65e−22未満だった。
[算術の範囲と次の停止条件対照](mml_cross_language_development_check.md#尤度差の安定計算と高精度照合)
を参照。今回は新しい最適化ではなく、元のABNORMAL、求積精度不足、推論資格は保持する。

追記（2026-09-14、終了状態の診断）: 保持したABNORMALを同じ設定で再現した。
50回の直線探索要求で上限に達し、独立した60／90桁計算では、ある探索点の真の改善4.98e−14を
倍精度が4.55e−13の悪化として返していた。局所Newton修正は最大1.26e−7だが、失敗は保持する。
正常終了した対照でも要求勾配精度の未達や積分精度不足があり、これらを別々に判定する。
[診断結果と既存契約の整理](mml_cross_language_development_check.md#異常終了の再現と数値精度の判定契約)
を参照。求積次数・推定核・推論資格の変更はない。

追記（2026-09-14、範囲×密度・実機対照）: ±12／±16／±20×間隔0.1／0.05を検証した。
広い裾では細分化だけで不足は解消せず、±16から今回の得点目安を満たした。
同じ開始値を使ったTAM・ConQuest各8件も実行・独立R照合を完了した。
ただし、ConQuestの事前グリッド正規化項は自由SDに対して定数ではなく、近い校正値から
同じ有限目的関数の停留性を推定しない。Pythonの再始動1件のABNORMALは保持した。
[結果・記録修正・次工程](mml_cross_language_development_check.md#積分範囲と密度の対照およびtamconquestの実測)
を参照。次の追記は、その前段階の検証記録として読む。

追記（2026-09-14、独立条件）: 回答を編集しない新しい80人のPCMデータ4組で、
通常条件はGH121で事前の得点精度目安を満たしたが、裾の広い2組はGH181でも満たさなかった。
連続積分参照・独立R・アプリ計算の検査は通過し、低次数の開始値依存も1組で確認した。
±12・間隔0.1の固定グリッドも、広い裾では事後SDの精度が不足した。
[条件・結果・次工程](mml_cross_language_development_check.md#独立した通常条件と広い裾での求積検証)
を参照。今回はTAM/ConQuestの新規ネイティブ実行ではない。

追記（2026-09-14、更新順序・返却点）: ConQuestの構造座標の更新は、古い事後重みと新しい
求積点で1巡する計算に近く、19箇所で最大1.946e−6の差だった。返却設定の4条件対照では、
最終点を返す設定は履歴を変えず、既定設定も全履歴の最良尤度点を保証しなかった。
[構造更新と返却点の対照](mml_cross_language_development_check.md#conquest構造更新と返却点の対照)
を参照。共通の尤度と連続積分を比較基準とし、内部処理の再構成と推論資格を区別する。

追記（2026-09-14、開始値対照）: 直接最適化したGH解をConQuestの開始値にした8条件を実行した。
4次数とも最初の更新で有限GH尤度が悪化し、中心化式が分散更新を予測した。
低次数の最終解は開始値に依存し、181点は最終SDが一致しても勾配・求積誤差が残る。
[直接GH解からの開始値対照](mml_cross_language_development_check.md#conquest直接gh解からの開始値対照)
を参照。連続積分の尤度の変化は次数で方向が異なり、有限GHだけで正確性を決めない。

追記（2026-09-14、追加）: ConQuestの平均アンカーを外す2条件対照で、校正・得点を変えずに
パラメータ数−1の表示を解消できた。GH全1,543履歴行の再計算では、尤度と座標の記録時点が
1行ずれること、更新がほぼ止まっても有限GHのSD勾配が残ることを確認した。
[平均アンカー対照・GH全履歴・分散モーメントの診断](mml_cross_language_development_check.md#conquest平均アンカー対照とgh全履歴監査)
を参照。TAMとの分散更新の共通点は、ConQuestでは出力に基づく数値的証拠として扱う。

追記（2026-09-14）: ConQuestの校正を固定し、得点計算の`p_nodes` 3水準×10 seedと
同一条件の再実行を照合した。抽出数の増加でEAP・事後SDの誤差は減少したが、
200,000でも最大約0.00396・0.00160 logit残る。これは得点用MCの精度であり、
MMLのGH求積次数や推定の収束とは別の検査である。
[校正固定での得点MC精度と未解決の出力項目](mml_cross_language_development_check.md#conquest校正固定での得点mc精度)
を参照。

追記（2026-09-13）: 同じPCMストレス例で連続積分再推定とTAMの実機照合を実施した。
さらにTAMの停止条件・範囲×密度を診断し、ConQuestもユーザーの通常Terminalから7条件を実行した。
エージェントの起動経路におけるライブラリ読込み拒否は、製品全体の使用不能を意味しない。
[TAM更新監査・ConQuest実測結果](mml_cross_language_development_check.md#tam停止条件の追跡とconquest実測による追加確認)
を参照。以下は2026-09-11時点のレビューとして保存する。

確認日: 2026-09-11。ソース・公式資料・保存済み検証のレビューと、求積関数だけの実行照合。
新しいモデル推定、ConQuestの実行、独立した統計資格検証は実施していない。

**同じMMLモデルでも、有限個の点で近似した尤度は求積法によって異なる。**
最適化の収束、積分精度、標準誤差・区間の妥当性は別々に確認する必要がある。
今回の重要な収穫は、mfrmrに既存の比較証拠と感度確認機能があること、そして
点数を増やすだけでは解決しない範囲・重みの問題も確認できたことである。

## 確認した実体

| 対象 | 今回確認した版・範囲 |
|---|---|
| Pythonアプリ | `integration/unified-app`、計算ソースは `49f63c411a7d23e535dfc44c1de0ee34ba174a7e` |
| 開発中mfrmr | 指定されたルートの `development/`。`0.2.4.9000`、HEAD `6dfba8258403cb0ab2f86979e78b31519753e82f`、開発状態 |
| インストール済みmfrmr | `0.2.3.1`。今回の開発版ソースと別物なので、通常の `library(mfrmr)` を開発版の実行確認には使っていない |
| R・TAM | 実機 R `4.6.1`、TAM `4.3.25`。インストール済み名前空間の関数本体も確認 |
| ConQuest | 保存記録の `5.47.5`。ローカル実行ファイルのSHA-256は拡張計画記載のものと一致。今回の起動は未確認 |

mfrmrのソース・記録の基準ディレクトリは
`/Users/tohokusla/Dropbox/MFRM_Application/mfrmr/development`。
以下のmfrmrリンクは同じ親ディレクトリにあるこの読み取り専用の調査対象を指す。
ソースと古い実行記録の版を混同せず、今回の照合結果も過去の凍結研究に上書きしない。

## 共通の積分と、異なる数値近似

単位重み・一つの潜在能力を持つ比較可能なモデルでは、Personごとの反応ベクトルをまとめて

\[
\ell(\alpha,\beta,\sigma)
=\sum_p\log\int_{-\infty}^{\infty}
\underbrace{\prod_{j\in O_p}P(Y_{pj}=y_{pj}\mid\theta,\alpha)}_{L_p(\theta;\alpha)}
\frac1\sigma\phi\!\left(\frac{\theta-\mu_p}{\sigma}\right)d\theta,
\qquad \mu_p=X_p\beta
\]

を最大化する。`alpha` はファセット効果・ステップなど。欠測は観測集合 `O_p` から外す。
評定行ごとに積分してから積を取る式とは異なり、同じPersonの能力を共有する。
行内の反応重みと積分外のPerson重みも同じ操作ではないため、重み付き比較は別途対応を決める。

標準正規Gauss–Hermite（GH）の点 `z_q` と重み `w_q` なら、

\[
\ell_Q=\sum_p\log\sum_{q=1}^Q w_q L_p(\mu_p+\sigma z_q;\alpha).
\]

変数変換により正規密度とJacobianは重みに含まれる。ここで `dnorm` や `1/sigma` を
もう一度掛けると二重計上になる。積の過小化を防ぐ計算では、カテゴリーの対数確率を
Person内で足し、`log(w_q)` を加えてから `logsumexp` を取る。

| 対象 | 今回確認した求積 | 最適化・母集団の扱い |
|---|---|---|
| Python | `hermgauss` を標準正規へ変換し、点を `sigma` 倍。低水準API既定15点、前回の開発監査は31・61点 | 現行の自由SD経路はEM。共同停留性を確認するv2は開発用。母集団SDが動くと点も動く |
| mfrmr 0.2.4開発版 | Golub–Welschの固有値・固有ベクトルから標準正規GHを生成。公開 `fit_mfrm()` 既定31点 | 既定 `direct` は勾配法と必要時のpolishing。通常RSM/PCMは固定N(0,1)、母集団モデル有効時は `mu_p + sigma*z_q`。限定したEM・hybridもある |
| TAM 4.3.25の決定論的経路 | 既定 `nodes=seq(-6,6,len=21), snodes=0`。保存比較では31・61点等を明示した等間隔グリッド | 固定点で正規密度を評価するEM系。母集団平均・分散を固定するか推定するかを明示する |
| ConQuest | `gauss` はGH、`quadrature` は別方式。回帰変数なしでは前者、回帰変数ありでは後者が既定。保存比較は **`method=quadrature` を明示** | MMLはEM系。今回の記録から既定 `gauss` の結果まで検証済みとは扱えない |

TAMは `snodes>0` ならQMC、さらに `QMC=FALSE` なら通常のMonte Carloを選べる。
ConQuestの `quadrature` は点数と `minnode/maxnode` を持ち、既定範囲は−6～6、
`gauss` ではこの範囲指定は無視される。公式の呼び名が同じ「Gaussian quadrature」でも、
TAMの既定等間隔点をHermite多項式の根と同一視しない。
[TAM公式マニュアル](https://alexanderrobitzsch.r-universe.dev/TAM/doc/manual.html)、
[ConQuest公式コマンド参照](https://conquestmanual.acer.org/s4-00.html#estimate)

実機のTAM `tam.mml` → `tam_stud_prior(normalize=FALSE)` → `tam_calc_posterior` →
`tam_mml_compute_deviance` を追うと、1次元の決定論的経路の周辺確率は
`Delta * sum_q L_p(t_q) * dnorm(t_q, mu_p, sigma)` として評価される。
事後確率の行正規化と、事前密度をグリッド上で総和1に再正規化する処理は区別する。
後者を無断で追加すると、有限グリッドの母集団パラメータ依存の目的関数を変え得る。

mfrmrのGHは母集団の位置・尺度へ変換するが、Personの**事後モード・曲率に合わせる
適応的GHではない**。ファセットが複数あっても現在のモデルの積分は1次元である。
多次元化した場合の積分点数の増大を、ファセット数の増加と混同しない。

## 今回実行した求積関数の照合

Pythonの `gauss_hermite_normal` はASTから当該関数だけを、mfrmrはRの `parse()` から
同名関数の代入式だけを取り出して実行した。アプリの推定やインストール済み旧Rパッケージは使っていない。
点を昇順にそろえ、R出力のテキスト転送も含めて比較した。

| Q | 点の最大絶対差 | 重みの最大絶対差 | mfrmrでゼロになった重みの数 |
|---|---:|---:|---:|
| 31 | 1.42e−14 | 2.11e−15 | 0 |
| 61 | 5.33e−14 | 5.27e−15 | 4 |
| 121 | 5.68e−14 | 5.79e−15 | 36 |
| 181 | 5.33e−14 | 4.45e−15 | 74 |

Q31・Q61について、SDが0.5・1・2の場合の点の尺度変換と、正規分布の
重み総和・1次・2次・4次モーメントも検査した。点の絶対差1e−12、重み1e−13、
モーメント2e−11という今回の実装照合用許容差を満たした。統計的な許容差ではない。
標準正規GHの最大正の点はQ31で9.8934、Q61で14.4985。これらはGHの最外点であり、
等間隔法のような明示的な積分打切り境界とは異なる。

**新しく確認した注意点：微小重みのゼロと保存校正の要件が整合していない。**
Rの現在の固有ベクトル方式では、Q61の4点で重みが正の微小値からゼロになる。
対応するNumPy側重みの合計は約7.42e−37で、Q121・Q181でも約2.18e−36・2.47e−37。
通常の単位重みの反応尤度積は0～1なので、これらの項を落とす効果は絶対確率では小さい。
しかし非常に小さい周辺確率に対する相対誤差・対数尤度誤差は、この値だけでは保証できない。
この観測は過去のTAM差やPythonの停留性問題の原因を特定したものではない。

加えて、mfrmrの保存校正は生成した重みをそのまま格納する一方、検証時は
`any(weights <= 0)` を拒否する。Q61・121・181の今回生成した重みはこの述語に抵触し、
Q31は通った。**fit時の点数を増やすことと、保存校正のscoring点数を増やすことは別**である。
これは生成関数と検証条件の直接照合であり、保存校正API一式の新規実行試験ではない。
根拠: [生成・格納・検証](../../mfrmr/development/R/core-fixed-calibration.R)、
[GH生成関数](../../mfrmr/development/R/mfrm_core.R)。
対応時は重みを任意の小数に置き換えず、共通生成関数の数値安定性と高次数の回帰検査を検討する。
今回mfrmrには変更を加えていない。

微小重みと正重み要件の不整合は、旧インストール版をロードせず次で再確認できる。
`Rscript --vanilla` に渡し、最後の表を確認する。

```r
src <- "/Users/tohokusla/Dropbox/MFRM_Application/mfrmr/development/R/mfrm_core.R"
env <- new.env(parent = baseenv())
for (e in parse(src)) {
  if (is.call(e) && identical(e[[1]], as.name("<-")) &&
      identical(e[[2]], as.name("gauss_hermite_normal"))) eval(e, env)
}
q <- c(31L, 61L, 121L, 181L)
zeros <- vapply(q, function(n) sum(env$gauss_hermite_normal(n)$weights == 0), integer(1))
stopifnot(identical(zeros, c(0L, 4L, 36L, 74L))) # 今回の実機結果の再現検査
data.frame(q = q, zero_weights = zeros, strictly_positive = zeros == 0L)
```

今回の実行に使ったファイルのSHA-256:

- Python `streamlit_app.py`: `747150dfcf1e2429bc6a95add53ed0d28d535691a4f03e24064e4b53e94d3c17`
- mfrmr `R/mfrm_core.R`: `b2f78488f251f80f3a3f705b10c41631cbe7e6ba5b3e1cca284cf5660a1000e1`

## mfrmrの既存検証から使える根拠

以下は**保存済みの過去の観測結果**。新規実行や現HEAD全体の合格宣言ではない。

| 記録 | 結果と意味 |
|---|---|
| [TAM基本条件の再照合](../../mfrmr/development/inst/validation/tam-mml-core-current-head-record-0.2.4.md) | RSM/PCMの単純な対応条件ではQ61のdeviance差が約1e−12。モデル・座標を対応できる限定的な根拠 |
| [RSMストレス比較](../../mfrmr/development/inst/validation/tam-mml-release-stress-record-0.2.4.md) | 21データセット・42比較。mfrmrは42/42で収束を報告したが、ソフト間基準は12/42、Q31→Q61の安定性基準は6/21だけが通過。TAMの42/42は反復上限未到達の記録であり、共同スコア検査とは異なる |
| [密度・裾範囲の追加診断](../../mfrmr/development/inst/validation/tam-mml-density-diagnostic-record-0.2.4.md) | Q121→181の変化は21/21で既存review基準内。それでもソフト間一致は17/21。残る例でTAMの範囲を広げると差が縮小 |
| [PCM条件付きストレス比較](../../mfrmr/development/inst/validation/tam-pcm-mml-conditional-stress-record-0.2.4.md) | 15データセット。ソフト間基準の通過はQ31で3/15、Q61で6/15、Q181で15/15。Q31→61のmfrmr最大EAP変化は0.0583、事後SD変化は0.0731。RSMだけの現象ではない |
| [ConQuest・TAMのアルゴリズム監査](../../mfrmr/development/inst/validation/external-mml-algorithm-correlation-audit-record-0.2.3.md) | 明示したConQuest `quadrature` とTAMは固定グリッドMML/EMの共通部分を持つ。mfrmr既定の直接最適化と同じアルゴリズムではない。高相関は同等性の基準ではない |
| [対数中心化した連続積分](../../mfrmr/development/inst/validation/conquest-p2-log-centered-continuous-oracle-observation-record-0.2.3.md) | 凍結された13条件でモード中心化・裾誤差を確認した数値基準にGHが一致。以前の連続積分との差が残った例もあるため、「連続積分」と呼ぶだけで正しい基準とはしない |

裾範囲の具体例では、Q301を保ったままTAMの範囲を−6～6から−8～8に広げると、
強制極端反応の1条件のdeviance差は約2.90e−4から1.92e−10になった。
点を密にすることと裾を広げることを分ける必要を示す。結果を見た後の機序診断なので、
元の12/42や6/21を合格に書き換えない。またQ181を万能の既定値にする根拠でもない。

連続積分の基準はモード探索、対数中心化、裾の評価を伴うが、確率計算をmfrmr本体と共有している。
独立ソフトによるモデル検証とは異なり、報告された積分誤差も区間演算による厳密証明ではない。
Pythonへの再利用時はR側で同じ数値を見るだけでなく、反応確率・同定・尤度の独立照合を残す。

## 0.2.4で実装済みの対応と、Python側への含意

mfrmrの [修正記録](../../mfrmr/development/inst/validation/mml-quadrature-remedy-record-0.2.4.md)
と現在の [公開API](../../mfrmr/development/R/api-quadrature-sensitivity.R) を照合した。
`mml_quadrature_sensitivity()` はRSM・PCM・限定GPCMについて、同じデータ・モデルの再推定から
尤度、構造パラメータ、確率、EAP、事後SD等の変化を示す。既定 `c(31,41)` は比較の出発点であり、
十分な精度の宣言ではない。自動の安定性閾値を持たず、推論資格も昇格させない。

[保存校正の抽出](../../mfrmr/development/R/api-calibration.R) はこのreviewと、その中の
厳密に同じ最高次数fitを要求する。これは確認手順の強制であり、数値安定性の自動認定ではない。
推定と得点計算の積分は別に評価する設計になっており、現在のscoring既定は31点である。

母集団分散の座標も対応させる必要がある。mfrmrは `zeta=log(sigma^2)`、Python v2は
`eta=log(sigma)` を使うため、`zeta=2*eta`。同じ他パラメータ座標ならスコアは
`d ell / d eta = 2 * d ell / d zeta` となり、Hessianにも変換が必要である。
mfrmrの感度出力のSD標準誤差は、共同共分散の該当成分から
`0.5*sigma*sqrt(Var(zeta))` を計算する診断値である。正則化状態も記録され、公開SE資格を
上書きしない。Pythonで撤去した「他パラメータ固定の曲率をProfile SEと呼ぶ」計算とは区別する。

Python側の次の作業は、既存v2・R照合・感度アダプターを再利用し、次の順に限定する。

1. **同じ有限求積目的関数での停留性**：構造効果とSDを同時に検査する。
   SD依存の移動点を含めて微分し、EMの終了だけでは合格にしない。
2. **積分精度**：既存Q31/Q61開発確認を出発点に、同じデータで尤度・確率・EAP・事後SD・
   母集団SDの変化を調べる。TAMは範囲と密度、GHは次数と微小重みを記録する。
   高次数の相互一致だけで十分とせず、必要な条件では監査した連続積分も使う。
3. **独立実装と推論**：同定・切片・平均・分散・ステップ・カテゴリー支持・重み・
   尤度定数を対応させる。EAP同士を比較し、WLEとは分ける。SE・区間・被覆率は別ゲートに残す。
4. **高速化とUI**：上記を保存した上でJAXを評価する。画面では「推定の終了」「積分点を
   変えた結果の動き」「推論に使える範囲」を順に示し、推定と得点計算の点数を区別する。

前回のPython RSM/PCMのQ31/Q61検査は、ゆるい開発用条件での確認であり、今回のmfrmrの
証拠を引用して本番資格に昇格させない。JAXの高速化は高精度の積分を実行しやすくする候補であり、
求積誤差を自動的に消すものではない。NumPyroの導入もこのMML検査の前提ではない。

2026-09-12の後続作業として、[Pythonと独立R計算の開発照合](mml_cross_language_development_check.md)
を実行した。既存の24 Person例で同一点の尤度・勾配・得点を比較し、Q121/Q181と
連続積分を追加した。これは本レビュー後の新規開発結果であり、外部ソフト全般の同等性や
mfrmrのゼロ重み問題の修正を宣言するものではない。
