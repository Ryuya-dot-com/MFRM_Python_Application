# MFRM Streamlit Application dependencies
# Install with: python3 -m pip install -r requirements.txt

numpy>=1.24
pandas>=2.0
scipy>=1.10
plotly>=6.1.1
kaleido>=1.0
# Chrome-free PNG fallback for the Publication Document (Word / PDF /
# HTML). When kaleido cannot reach a Chrome binary (common on
# Streamlit Community Cloud), matplotlib renders the figures instead.
matplotlib>=3.8
streamlit>=1.54
openpyxl>=3.1
# Publication-ready Word / PDF export (Report tab → Publication Document).
python-docx>=1.0
reportlab>=4.0
# Posterior Viewer mode: inspect externally-produced posterior draws
# (CmdStan CSV / Apache Parquet / ArviZ NetCDF) with trace, ridge, pair,
# and forest plots plus HMC diagnostics.
arviz>=0.17
netcdf4>=1.6
pyarrow>=15.0
